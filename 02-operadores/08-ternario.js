// expresion ? 'Si es true' : 'Si es false'
// Si la condicion se cumple, devuelve el primer valor. Sino,

let edad = 18;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No ingresar';

console.log(acceso);